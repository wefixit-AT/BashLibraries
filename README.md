# BashLibraries
Useful Libraries for bash

## checkExitStatus.sh
Checks the exit status and exit the script.

## checkPid.sh
Check if a given Pid is set

## logging.sh
Useful for logging which you can enable/disable and add a prefix
